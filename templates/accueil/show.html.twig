{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
    <h1>Accueil</h1>

    <img src="/home/{{ accueil.image }}" width="50%">
    <table class="table">
        <tbody>
            <tr>
                <th>Title</th>
                <td>{{ accueil.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ accueil.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('accueil_index') }}">back to list</a>

    <a href="{{ path('accueil_edit', {'id': accueil.id}) }}">edit</a>

    {{ include('accueil/_delete_form.html.twig') }}
{% endblock %}
