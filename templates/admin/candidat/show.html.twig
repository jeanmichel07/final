{% extends 'base.html.twig' %}
{% block body %}
    <br>

    <div class="container">
        <img width="260px" height="200px" src="/candidat/{{ candidat.photo }}">        <h2><b>{{ candidat.nom }}</b></h2>
<br>
        <table class="table">
            {% set sexe = candidat.candidatSexe == null? '': candidat.candidatSexe.label %}
            <tr><th>Sexe :</th><td>{{ sexe }}</td></tr>
            <tr><th>Né(e) le :</th><td>{{ candidat.dateNaissance |date('d-m-Y') }}</td></tr>
            {% set situation  = candidat.situation == null? '': candidat.situation.label %}
            <tr><th>Situation familier :</th><td>{{ situation }}</td></tr>
            <tr><th>Ville :</th><td>{{ candidat.ville }}</td></tr>
            <tr><th>Pays :</th><td>{{ candidat.pays }}</td></tr>
            <tr><th>Email :</th><td>{{ candidat.email }}</td></tr>
            <tr><th>Telephone :</th><td>{{ candidat.telephone }}</td></tr>
            <tr><th>Adresse :</th><td>{{ candidat.adresse }}</td></tr>
        </table>

        <a href="javascript:history.back()" class="btn btn-warning">Retour</a>
    </div>

{% endblock %}